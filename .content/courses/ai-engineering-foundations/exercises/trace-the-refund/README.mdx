`refund(order, amount)` must return a copy of the order with the refund recorded and the status updated. A full refund makes the order `refunded`; a partial refund keeps it `paid` but records the amount. One of these paths is wrong. Find it by tracing `status`, not by guessing.
