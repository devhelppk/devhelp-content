`count_words(text)` should return a dictionary mapping each lower-cased word to how many times it appears, ignoring punctuation. The starter lower-cases the text but forgets about punctuation and empty strings; trace one input through it before you change anything.
